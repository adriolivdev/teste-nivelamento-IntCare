// Importo a função createApp do Vue (usado no Vue 3)
import { createApp } from 'vue';
// Importo o componente raiz da aplicação
import App from './App.vue';

// Crio a instância do Vue e monto a aplicação no elemento com id "app"
createApp(App).mount('#app');